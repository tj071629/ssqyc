# 排列5 留出段超参搜索（非自动生成新特征）

## 口径说明
- 仅在**固定特征集**上搜索 `learning_rate` 与 `decay`；不会从数据中自动发明新规则字段。
- 训练段：`0 .. len-holdout-1` 逐期自适应；留出段：对最后 `holdout` 期做**继续 walk-forward**（每期仍更新权重），仅用留出段的排名指标做比较。
- 随机序列上指标改善可能是噪声；若某组参数明显更优，应在**另一段未参与调参的留出期**上复核。

## 数据与切分
- 文件：`docs\p5.md`
- 使用期数：7598（全文件 7598；tail=all）
- holdout：300；min_history：100；grid：quick

## 留出段直选一等奖口径（Top1）
- 当前脚本在留出段每期只取模型**排名第 1** 的一注作为虚拟投注对象；若该期开奖号码的得分排名 **= 1**，则等价于「买这一注中直选一等奖」（五位全中）。
- 下表「一等奖次数」即留出期内 `rank == 1` 的期数；「最佳排名」为留出期内开奖号的最靠前名次。

## 结果（按留出段平均排名升序）

| 方案 | lr | decay | 留出均值排名 | 留出中位 | 前100占比 | 前1000占比 | 一等奖次数 | 最佳排名 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| decay_loose | 0.035 | 0.999 | 49170.0 | 48948.0 | 0.00% | 1.00% | 0 | 352 |
| baseline | 0.035 | 0.997 | 49553.8 | 50864.0 | 0.00% | 2.00% | 0 | 197 |
| lr_high | 0.055 | 0.997 | 50086.6 | 50477.0 | 0.00% | 0.33% | 0 | 752 |

## 当前推荐（仅相对本切分）
- 参数：`learning_rate=0.035`, `decay=0.999`（标签 `decay_loose`）
- 留出段平均排名 **49170.0** / 100000（越小表示开奖号在模型排序中越靠前，仍不等于提高真实中奖率）。
- 留出段直选一等奖（Top1）次数：**0** / 300；最佳排名：**352**。
